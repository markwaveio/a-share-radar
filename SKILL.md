---
name: a-share-radar
description: Install, run, and operate the A-Share Radar — an automated A-share market scanner that generates four daily Excel reports (multi-timeframe KDJ across 10 periods, volume coefficient-of-variation anomalies, news-mention heat, sector capital-flow ranking). Use when the user wants to 安装/部署 A股行情雷达, 跑 KDJ 全周期表, 找放量异动股, 看板块资金热度, 装每日定时盯盘, or asks to set up automated A-share screening reports.
---

# A股行情雷达 · 引导安装与使用

这个 skill 把 A 股全市场的盯盘筛选变成每交易日自动生成的四张 Excel。
它负责三件事：**引导安装 → 验证跑通 → 教会读表**。

核心边界，任何时候都不要越过：

```
只做数据加工。不给投资建议，不预测涨跌，不接任何交易接口。
产出是候选池，不是买卖信号。
```

---

## 什么时候用这个 skill

用户说这些的时候：

- 「安装 / 部署 A股行情雷达」「装一下这个盯盘工具」
- 「跑一下 KDJ 全周期表」「看看今天哪些股票放量了」
- 「今天资金流向哪个板块」「装个每天自动出报表的」
- 「这个报表怎么看」「为什么这一列是空的」
- 「定时任务没跑」「失败数怎么这么多」

不用于：荐股、预测涨跌、下单、回测收益。

---

## 一、引导安装（首次）

按顺序执行，**每一步通过再做下一步**。任何一步失败就停下来告诉用户具体原因，不要跳过。

### 步骤 1：环境检查

```bash
python3 -V
python3 -c "import pandas, numpy, openpyxl, requests; print('依赖齐全')"
```

- Python 需要 3.9+
- 缺依赖就 `python3 -m pip install -r requirements.txt`
- macOS 系统自带的 python3 通常已有这些包，先检查再装，别无脑安装

### 步骤 2：网络连通性

```bash
curl -s -o /dev/null -w "eastmoney: %{http_code}\n" \
  'https://push2delay.eastmoney.com/api/qt/clist/get?pn=1&pz=2&po=1&np=1&fltt=2&invt=2&fid=f12&fs=m:1+t:2&fields=f12,f14'
curl -s -o /dev/null -w "tencent:   %{http_code}\n" \
  'https://ifzq.gtimg.cn/appstock/app/fqkline/get?param=sh600000,day,,,3,qfq'
```

两个都 200 最好。**只要腾讯是 200 就能跑** —— 项目有多源降级，东财挂了会自动切。

如果两个都不通：
- 检查本机代理。Python 默认读 `HTTP_PROXY`，代理挂了会全线失败
- 让用户试 `RADAR_USE_PROXY=0 python3 run.py --task 1 --limit 5`

### 步骤 3：离线自检（不需要网络）

```bash
python3 tests/test_indicators.py
```

必须 **11 个测试全绿**。这验证的是 KDJ 递推算法、一字板除零、数据不足返回 None、
成交量 CV 基准期排除逻辑、120 分钟合成规则。

不绿就不要继续 —— 指标算错不会报错，只会安静给出错误结论。

### 步骤 4：链路自检

```bash
python3 run.py --task 1 --limit 30
```

看日志确认四件事：

| 日志 | 含义 |
|---|---|
| `全市场 A 股共 55xx 只` | 股票池拉到了 |
| `扫描完成：成功 30，失败 0` | 抓取正常 |
| `数据源命中：tencent 90次 tencent_min 90次` | 90+90=180=30只×6周期，完整 |
| `已生成 .../01_KDJ全周期_*.xlsx` | 输出正常 |

**命中数对不上就是有周期缺失**，去查 `radar/providers.py` 的优先级链。

### 步骤 5：数据可信度验证（不要跳过）

打开生成的 Excel，挑 3~5 只股票，让用户在自己的行情软件里对照日线 KDJ(9,3,3)。

| 差值 | 判断 |
|---|---|
| < 0.05 | 通过 |
| 0.05 ~ 1 | 可接受，记录下来 |
| > 1 | **不通过**，查 README「KDJ 和行情软件对不上」那节 |

如果用户装了富途 OpenD（`127.0.0.1:11111` 在监听），可以用它做自动交叉验证 ——
富途是完全独立的数据商，两边一致就能排除数据源风险。

### 步骤 6：跑全部四个需求

```bash
python3 run.py --task all
```

全市场约 15~25 分钟。**第一次可以先用 `--limit 300` 跑个 1 分钟版本**让用户看到完整的四张表，
确认结构没问题再跑全量。

### 步骤 7：装定时任务

```bash
./scripts/install_launchd.sh install
./scripts/install_launchd.sh status
```

装完要告诉用户：

- 11:40 午盘批只跑需求 1（成交量和资金流要等收盘才完整）
- 15:15 收盘批四个全跑（15 分钟缓冲是等资金流数据落库）
- 非交易日会自动跳过 —— 它去问上证指数今天有没有 K 线，不维护会过期的节假日表
- 开机不补跑 —— 错过的批次数据已失去时效，补跑会覆盖出错误时点的快照

### 步骤 8：告诉用户"接下来会发生什么"

```
需求 4（板块热度）第一天只有 1 列数据，A-3/A-2/A-1 三列是空的。
没有接口能稳定给多日历史，所以改成本地按日累积。
每天跑一次，3 个交易日后自动补满。
```

这一步必须讲，否则用户会以为工具坏了。

---

## 二、日常使用引导

### 四张表的阅读顺序

**从板块往下，不要从个股往上。** 从 5200 只 KDJ 开始筛会得到几百只金叉股，然后无从下手。

```
1. 04 板块热度    → 挑 2~3 个 A-3→A 连续升温的方向（跳过属性类板块）
2. 02 放量TOP200  → 这些方向里，放量倍数 > 3 且 基准期CV < 0.6 的
3. 03 升温TOP100  → 消息面跟上了吗（先看基准期条数，< 5 直接跳过）
4. 01 KDJ全周期   → 月/周/日是否同向
   → 缩到 10~20 只写进 data/watchlist.txt
```

### 需求 3 的正确用法

全市场逐个搜资讯约 2.2 万次请求会被限流。**先筛后查**：

```bash
# 把第 1~2 步筛出的候选写进 data/watchlist.txt，每行 `代码,名称`
python3 run.py --task 3 --sentiment-universe watchlist
```

十几只只要几秒，又快又准，还避开限流。这是最实用的用法，要主动教给用户。

### 必须提醒的读表陷阱

| 陷阱 | 说明 |
|---|---|
| 属性类板块霸榜 | 融资融券 99.9%、沪股通 59% 覆盖大半个市场，没有方向性信息，跳过 |
| 空值 ≠ 0 | 数据不足时留空。J 值 0 会被读成"超卖" |
| 「本日」是活的 | 午盘批和收盘批的「本日」必然不同，这是定义决定的，不是 bug |
| 只看放量倍数 | 平时 CV 就 1.5 的股票放量 3 倍是常态，必须结合基准期 CV |
| 升温倍数很高 | 先看基准期条数。2 条变 20 条也是 10 倍，但没有意义 |
| 板块占比之和 ≠ 100% | 概念板块互相重叠，实测约 1700%，正常 |
| Excel 筛选打不开 | 需求 1 是两行合并表头，要选中**第 2 行**再开筛选 |

### 每天 30 秒健康检查

```bash
ls -la data/output/ | grep $(date +%Y%m%d)
grep -E "扫描完成|数据源命中|完成，耗时" data/logs/run_$(date +%Y%m%d).log
```

**失败数 > 500 只的那天，数据不可用**，不要用"至少大部分是对的"说服自己 ——
你不知道错的是哪几百只。

---

## 三、维护与排障

排查顺序永远是**从外往里**：launchd 日志 → 脚本日志 → Python 日志。

| 症状 | 先查什么 |
|---|---|
| 一个文件都没生成 | 手工 curl 验证接口；检查本机代理 |
| 手动跑没问题、定时跑失败 | PATH。launchd 的环境变量和终端不一样，看 `data/logs/launchd.*.err.log` |
| 失败数突然涨到几百 | 接口变了或被限流。调低 `concurrency` 到 6，`qps_limit` 到 15 |
| KDJ 对不上行情软件 | 复权方式 → 平滑算法 → 初值 → 历史长度，按这个顺序 |
| 需求 4 历史列是空的 | 正常。本地按日累积，跑满 4 个交易日才满 |
| 需求 3 整张表全空 | 检查搜索接口返回；确认没有双重 URL 编码 |

参数全部集中在 `radar/config.py`，改任何指标都要重跑 `python3 tests/test_indicators.py`。

---

## 四、回答用户问题时的原则

**关于成本**：数据拉取是纯脚本执行，**0 token 消耗**。装上定时任务后完全不经过模型。
token 只花在建和改的时候。

**关于准确性**：指标是对已发生事实的统计描述，不包含未来信息。
说不清口径的数字不要放进决策。

**关于建议**：不要给出「该买」「该卖」「会涨」这类判断。
可以说「这只股票同时满足了资金流入、放量、KDJ 三层同向」，
不能说「这只股票值得买」。用户问荐股时，把话题拉回筛选条件本身。

**关于数据源**：这些是第三方公开接口，没有官方文档也没有服务承诺。
接口失效时不要硬扛，先用 curl 确认是接口问题还是代码问题。

---

## 参考

| 文档 | 用途 |
|---|---|
| `README.md` | 运作原理 + 完整使用维护说明 |
| `INSTALL.md` | 安装细节与验收清单 |
| `docs/methodology.md` | **指标口径唯一权威**，代码与它不一致时改代码 |
| `docs/data-source.md` | 各接口字段顺序与已知故障形态 |
| `guide/` | 使用手册，14 节 |
